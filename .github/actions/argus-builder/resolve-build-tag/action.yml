name: argus-builder/resolve-build-tag
description: Resolve the Docker image tag from the trigger SHA

outputs:
  image_tag:
    description: The resolved image tag (e.g. sha-abc1234)
  trigger_sha:
    description: The full trigger SHA

runs:
  using: 'node24'
  main: 'dist/index.js'
