{
  "name": "codeowners-approval-check-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action that passes only when every changed file with a CODEOWNERS rule is authored or approved by a code owner",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest --config=../../../jest.config.js --rootDir=./",
    "build": "ncc build src/main.ts --source-map --minify",
    "lint": "eslint --max-warnings=0 src/",
    "lint:fix": "eslint --fix src/",
    "eslint": "eslint"
  },
  "author": "CZI",
  "license": "MIT",
  "dependencies": {
    "codeowners-utils": "^1.0.2"
  }
}
